import React from 'react';
import Form from '../components/Form';

const Home = () => {
    return (
        <div className="container global-width">
            <Form />
        </div>
    );
};

export default Home;